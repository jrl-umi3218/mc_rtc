{
  "type": "object",
  "properties":
  {
    "type": { "enum": ["kinematics", "dynamics"] },
    "robot": { "$ref": "../common/ConstraintSet_robot.json" },
    "damper":
    {
      "type": "array",
      "items":
      {
        "type": "number",
        "minItems": 3,
        "maxItems": 3
      },
      "description": "Joint Limits Damper parameters [interaction disstance, safety distance, offset]"
    },
    "velocityPercent":
    {
      "description": "Percentage of the max joint velocity to use. Ignored if damper is absent.",
      "type": "number",
      "default": "0.5"
    }
  },
  "required": ["type"]
}
